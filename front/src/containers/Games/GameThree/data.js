const randomNumber = () => {
  return( 
    Math.floor(Math.random(999) * 999)
  )
}

export const data = [ randomNumber(),
  randomNumber(),
  randomNumber(),
  randomNumber(),
  randomNumber(),
  randomNumber(),
  randomNumber(),
  randomNumber(),
]


